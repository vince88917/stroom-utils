#!/bin/bash
#
# For SystemD this sources /etc/bashrc so that we have the right
# environment, including PATH and JAVAHOME, for running STROOM
#
source /etc/profile.d/stroom.sh
/opt/{{ appType }}-{{ stroomVersion }}/{{ script }}
